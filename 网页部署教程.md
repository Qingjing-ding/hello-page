# 网页部署教程:让任何人都能访问你的"你好"网页

> 这份教程写给**完全没接触过网站部署的人**。
> 你只需要一个只有两个字的 HTML 网页,跟着做,就能得到一个发给全世界都能打开的网址。
> 两条路线任选:**路线 A 免费、10 分钟**;**路线 B 约 100 元/年 + 1~2 周等待**,国内访问秒开。

---

# 零、先懂 3 个词,再看教程

| 词 | 意思 | 类比 |
|---|---|---|
| **网页文件** | 就是一个文本文件,比如 `index.html`,用记事本都能编辑 | 一张海报的电子稿 |
| **部署** | 把网页文件放到一台 24 小时开机、连着互联网的电脑上,让别人能访问 | 把海报贴到菜市场门口 |
| **域名 / 网址** | 访问你网页用的地址,如 `xxx.github.io/hello-page` | 菜市场门口的"摊位编号" |

**重要认知:纯静态网页不需要"打包"!** 不需要 PyInstaller、不需要编译。
部署 = 把 `index.html` 这个文件放到公网上的服务器,完事。

**首页文件必须叫 `index.html`**(全行业默认规则):访问一个网址时,服务器自动返回这个文件。
如果你的中文显示乱码,检查文件里有没有 `<meta charset="utf-8">` 这一行。

你的网页文件示例(本仓库 `index.html` 的极简版):

```html
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>你好</title>
</head>
<body>
    <h1>你好</h1>
</body>
</html>
```

两条路线怎么选:

| | 路线 A:GitHub Pages | 路线 B:国内服务器 + 域名 + 备案 |
|---|---|---|
| 花费 | **免费** | 约 100~200 元/年(域名 + 学生云服务器) |
| 耗时 | **10 分钟** | 操作 1 小时 + 备案等待 1~2 周 |
| 国内访问速度 | 一般,偶有抽风 | **秒开** |
| 适合 | 学习、作业、作品展示 | 面向国内用户的正式网站、带后端的项目 |

---

# 路线 A:GitHub Pages(免费,推荐从这里开始)

GitHub 提供免费的静态网页托管,网址形如 `你的用户名.github.io/仓库名`。
分两种做法:**A1 纯网页点击**(完全不用命令行)和 **A2 命令行**(更快)。

## A0 注册 GitHub 账号(已有账号跳过)

1. 打开 https://github.com/signup
2. 输入邮箱 → 设置密码 → 起用户名(**这个用户名会成为你网址的一部分**,建议用纯小写字母)→ 邮箱收验证码完成验证。

✅ 验证:能登录 https://github.com 并看到右上角自己的头像。

## A1 路线一:纯网页点击,全程不碰命令行

### 第 1 步:新建仓库

1. 登录 GitHub,点右上角 **`+`** 号 → **New repository**;
2. Repository name 填 `hello-page`;
3. 选择 **Public**(免费账号的 Pages 只对公开仓库生效);
4. 勾选 **Add a README file**(让仓库不为空);
5. 点绿色按钮 **Create repository**。

### 第 2 步:上传网页文件

1. 在仓库页面点 **Add file → Upload files**;
2. 把你电脑上的 `index.html` **拖进网页中间的虚线框**(或者点 `choose your files` 选择文件);
3. 拉到页面底部,点绿色按钮 **Commit changes**。

✅ 验证:仓库文件列表里出现了 `index.html`,点开它能看见代码。

### 第 3 步:打开 Pages 开关

1. 仓库页面点 **Settings**(顶部一排标签里);
2. 左侧菜单点 **Pages**;
3. **Build and deployment** 区域,Source 选 **Deploy from a branch**;
4. Branch 下拉框选 **main**,右边的目录选 **/(root)**,点 **Save**。

✅ 验证:页面顶部出现绿色提示 `Your site is live at https://你的用户名.github.io/hello-page/`
(如果还是灰色的提示"正在构建",等 1~2 分钟刷新页面)。

### 第 4 步:访问你的网站

浏览器打开(用户名部分**全部小写**):

```
https://你的用户名.github.io/hello-page/
```

看到你的"你好"——恭喜,你的网页已经在全球互联网上了。

### 以后怎么改内容(不碰命令行版)

仓库页面点开 `index.html` → 点右上角**铅笔图标** → 改代码 → 点 **Commit changes** → 约 1 分钟自动生效。

## A2 路线二:命令行(装过 Git 的用这个,快)

电脑装 Git:https://git-scm.com/download/win (安装时全部默认下一步即可),然后在 PowerShell 里:

```powershell
mkdir D:\Study\hello-page; cd D:\Study\hello-page
# 用记事本创建 index.html(内容见本文最上方),保存后:
git init ; git add index.html ; git commit -m "我的第一个网页"
# 在 GitHub 网页上按 A1 第 1 步建好空仓库 hello-page 后:
git remote add origin git@github.com:你的用户名/hello-page.git
git push -u origin main
```

然后按 A1 的第 3、4 步打开 Pages 开关并访问。

> 命令行推送需要先在 GitHub 配置 SSH 密钥或用登录凭据;嫌麻烦就直接用 A1 的纯网页方式,效果完全一样。

## 路线 A 常见问题

| 问题 | 原因与解法 |
|---|---|
| 打开是 404 | Pages 还在构建(等 1~2 分钟);或网址拼写错了——**用户名部分必须全小写** |
| 改了代码没变化 | 浏览器缓存:**Ctrl+F5** 强制刷新 |
| 提示 404 但仓库有文件 | 首页文件名必须是 `index.html`(不是 `Index.html`、不是 `home.html`) |
| Settings 里找不到 Pages | 仓库必须是 **Public**;且要先有至少一个文件 |
| 仓库改成私有后网站 404 | 免费账号 Pages 只支持公开仓库,**转私有会把 Pages 站点整个删除**;改回公开后还要去 Settings → Pages 重新开启一次(不会自动恢复)。想"代码私有 + 网站公开"需升级 GitHub Pro,或把网页单独放一个公开仓库 |
| 国内打开慢/偶尔打不开 | `github.io` 在国内的速度波动属于正常现象,介意就走路线 B |

---

# 路线 B:国内云服务器 + 域名 + 备案(任何中国人秒开)

> 流程:买域名 → 买云服务器 → ICP 备案(等 1~2 周)→ 服务器上放网页 → 域名解析 → 上线。
> 花费:域名几十元/年 + 学生云服务器约 100 元/年。**备案本身免费**。

## B1 买域名(网站的"门牌号")

二选一(都是大厂,流程一样):

- 腾讯云域名注册:https://buy.cloud.tencent.com/domain
- 阿里云域名(万网):https://wanwang.aliyun.com/domain/

操作:

1. 搜索框输入想要的域名前缀(如 `myname-site`),挑一个便宜后缀(`.com` 首年约 55~80 元,`.top`/`.xyz` 更便宜);
2. 加入购物车 → 结算。**购买时会要求实名认证**:填身份证信息,提交后等审核通过(几小时到 1 天);
3. ✅ 验证:域名控制台里域名状态显示"已实名"。

> 备案要求域名实名信息入库(一般要等 1~3 天)才能开始提交备案,所以域名买完先去做 B2。

## B2 买云服务器(放网页文件的 24 小时电脑)

学生/新手首推**轻量应用服务器**(套餐固定、按年买便宜、控制台简单):

- 腾讯云轻量应用服务器:https://cloud.tencent.com/product/lighthouse
  (学生优惠入口:https://cloud.tencent.com/act/campus ,常有百元级年付套餐)
- 阿里云轻量应用服务器:https://www.aliyun.com/product/swas

操作:

1. 选**地域**:离你近的(上海/广州/北京都行);
2. 选**镜像**:选 **Linux / Ubuntu Server 22.04 LTS**(后面 B4 的命令按这个写;新手不用怕,大部分操作在网页后台完成);
3. 套餐选最低配(2 核 2G 足够跑几十个静态网站);
4. **购买时长 ≥ 3 个月**——这是提交 ICP 备案的硬性条件之一;
5. 付款后进入控制台:找到你服务器的**公网 IP**(形如 `43.143.x.x`,记下来),并在控制台**重置 root 密码**(或设置密钥)。

✅ 验证:控制台服务器状态为"运行中",能查到公网 IP。

## B3 ICP 备案(政策要求,免费,等 1~2 周)

**为什么必须备案**:网站服务器在中国大陆、通过域名对外提供服务,工信部要求必须备案,否则访问会被阻断。这是流程,不是收费项目。

提交入口(在你买服务器的厂商那里提交):

- 腾讯云备案:https://cloud.tencent.com/product/ba
- 阿里云备案:https://beian.aliyun.com/

操作:

1. 备案控制台 → **开始备案** → 网站备案;
2. 填写:主体信息(个人身份证 + 手机号)、网站信息(域名、网站名称随便起但不能带敏感词、服务内容选"个人博客/展示类")、把域名绑定到你的轻量服务器;
3. 按提示完成**人脸核验**(手机视频);
4. 提交 → 厂商初审(1~2 天,可能电话回访)→ 提交管局审核(约 1~2 周);
5. 通过后你会收到短信和**备案号**(形如 `京ICP备2026XXXXXX号`)。

✅ 验证:收到管局备案通过的短信/邮件。

> 注意:备案期间域名**不能**解析到国内服务器对外提供服务;备案号下来后要挂在网站底部。

## B4 把网页放上服务器(两种做法)

### 做法 1:宝塔面板(推荐新手,全程网页点击)

[宝塔面板](https://www.bt.cn/new/index.html) 是一个服务器管理后台,装好后一切操作都是点鼠标。

1. 腾讯云控制台进入轻量服务器详情 → 点 **登录**(网页版终端,输入 root 和你设置的密码);
2. 去宝塔官网 https://www.bt.cn/new/index.html 复制 **Ubuntu 安装命令**(官网首页就有,一行),粘贴进终端回车,中途输入 `y` 确认;
3. 装完终端会显示**面板地址、用户名、密码**(记下来!);第一次还需在云控制台的"防火墙"里放行面板端口(它会提示端口号);
4. 浏览器打开面板地址登录 → 首次弹窗装 **Nginx**(选默认,等几分钟);
5. 面板左侧 **网站** → **添加站点**:域名填你的域名,根目录默认(形如 `/www/wwwroot/你的域名`);
6. 面板左侧 **文件** → 进入该站点根目录 → 删掉默认的 `index.html` → **上传**你的 `index.html`;
7. 回到云控制台,在**防火墙**规则里放行 **80 端口**(宝塔装站点时通常已自动加,检查一下)。

### 做法 2:纯命令行(4 条命令)

腾讯云控制台网页登录服务器后:

```bash
apt update && apt install -y nginx          # 装 Nginx 网页服务器
echo '<meta charset="utf-8"><h1>你好</h1>' > /var/www/html/index.html   # 放入网页
```

然后在云控制台**防火墙/安全组**里放行 **80 端口**(添加规则:TCP,80,允许所有来源)。

✅ 验证(两种做法通用):浏览器打开 `http://你的公网IP`,能看到"你好"。

## B5 域名解析 + 正式上线

1. 回到域名控制台(腾讯云 https://dnspod.cloud.tencent.com 或阿里云解析控制台);
2. 添加一条记录:**记录类型 A**,主机记录 `@`,记录值填服务器的**公网 IP**;
3. 等 10 分钟~2 小时(DNS 生效),浏览器访问 `http://你的域名`。

✅ 验证:网址栏输入域名,看到"你好"。把网址发给任何人测试。

> 记得在网站底部放一行你的备案号(政策要求),例如 `<a href="https://beian.miit.gov.cn">京ICP备2026XXXXXX号</a>`。

## 路线 B 常见问题

| 问题 | 原因与解法 |
|---|---|
| 备案被退回 | 网站名称含敏感词/与个人性质不符;按退回原因改了重提即可 |
| 访问 IP 打不开 | 防火墙没放行 80 端口(云控制台 + 宝塔两边都要放行) |
| 域名打不开但 IP 能 | 域名解析没生效(DNS 最长等 2 小时);或备案还没通过 |
| 中文乱码 | `index.html` 里必须有 `<meta charset="utf-8">` |

---

# 三种免费/低价方案补充说明

| 方案 | 国内可访问性 | 说明 |
|---|---|---|
| GitHub Pages(路线 A) | 一般,偶有波动 | 免费首选 |
| Vercel / Netlify | **基本打不开**(免费域名被墙) | 除非绑定自己的域名,否则不推荐国内用户 |
| 码云 Gitee Pages | — | 服务已暂停,别找了 |

---

# 总结:你现在会了什么

1. 纯静态网页的部署本质 = **把 `index.html` 放到公网服务器**;
2. 免费路线:GitHub 仓库 + Pages 开关 = 全球可访问的网址;
3. 正式路线:域名 + 国内服务器 + 备案 + Nginx/宝塔 = 国内秒开的网站;
4. 这两套路子将来可以复用:路线 A 挂任何静态作品(简历、作品集);路线 B 的服务器还能跑 Python 后端、数据库——一台机器什么都能部署。
